====== Use case: interactive =======

with stop_before(func) as e:
    cl.post('/url')

##
%%stop_before func

scenario
##

vars got injected


@post_hook(func)
def log_func(*args):
    # logging...

=============

hooks as class ?
discovered

class Hook(ReadOnly):
    func = SomeClass.ctx_mod_func

    def log_pretty(self):
        1

    def cond(self):
        1

    def post(self):
        1

    def pre(self):
        1


=== Use case: test =======

class Case:

    def setUp(self):
        with stop_before(func) as e:
            cl.post('/url')
        self.ctx = e.ctx

##################

functions, patches = bpts

get_inst(MyClass)


events
