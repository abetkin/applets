function based views

it = get_iter()

it.wait(Klass.method) <- s.m.

####

def some_method(self):
    super().some_method(stop_before=f)
    it.wait(Klass)


with stop_before(B.walk):
    1

# bug : repeating elements in ctx
