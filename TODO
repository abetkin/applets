function based views

# bug : repeating elements in ctx

#####

django examples
    serialize, deserialize, filter

__________
debugger: fix frame navigation


stop: any condition
    all / configuration

properties


res = stop_after(Serializer.is_valid)

if it's a greenlet, all is ok (make a method in MethodGreenlet?)
